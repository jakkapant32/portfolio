# Tum.dev | IT x Business Solution

Portfolio เว็บไซต์แนะนำตัว – Jakkapan (ตั้ม)  
**IT Business Solution Developer** | Full-stack + Sales + CRM Experience

## 🚀 รันโปรเจกต์

```bash
cd portfolio
npm install
npm run dev
```

เปิดเบราว์เซอร์ที่ `http://localhost:5173`

## 📦 Deploy บน Vercel

### วิธีที่ 1: เชื่อม GitHub (แนะนำ)
1. อัปโหลดโปรเจกต์ขึ้น GitHub (สร้าง repo แล้ว push โฟลเดอร์ `portfolio`)
2. ไปที่ [vercel.com](https://vercel.com) → Sign in with GitHub
3. กด **Add New** → **Project** → เลือก repo ของคุณ
4. Vercel จะตั้งค่าให้อัตโนมัติ (Build: `npm run build`, Output: `dist`)
5. กด **Deploy** — เสร็จแล้วจะได้ลิงก์ เช่น `https://your-project.vercel.app`

### วิธีที่ 2: ใช้ Vercel CLI
```bash
cd portfolio
npm i -g vercel
vercel
```
ทำตามคำถามในเทอร์มินัล (ล็อกอินครั้งแรก) จากนั้น Vercel จะ build และ deploy ให้

### Build เองก่อน (ถ้าใช้ host อื่น)
```bash
npm run build
```
ไฟล์จะอยู่ที่ `dist/` — อัปโหลดโฟลเดอร์นี้ไปที่ใดก็ได้

## ✏️ แก้ไขข้อมูลส่วนตัว

- **ลิงก์ Contact** (เบอร์, Email, GitHub, Facebook, LinkedIn, Line): แก้ใน `src/components/Contact.jsx`
- **Resume PDF**: วางไฟล์ `resume.pdf` ในโฟลเดอร์ `public/` จะได้ปุ่ม Download Resume ทำงาน

## 🎨 เทคโนโลยี

- React + Vite
- Tailwind CSS
- โทนสีดำ–ทอง (#FFC107)
- ฟอนต์ Kanit + Poppins

## 📂 โครงสร้าง

- หน้าเดียว (Single Page) มีทุกส่วน: Hero, About, Experience, Skills, Projects, What I Can Do, Contact
- Dark / Light mode
- Animation ตอนเลื่อนหน้า
